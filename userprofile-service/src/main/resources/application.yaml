spring:
  application:
    name: userprofile-service
  datasource:
    url: jdbc:mysql://localhost:3306/user_profile_service
    username: ashish
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
server:
  port: 8200